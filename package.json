{
  "name": "vtlsp",
  "version": "0.0.1",
  "private": true,
  "description": "VTLSP provides a complete solution to running a language server on a server, and stream messages to an editor in your browser.  This is a monorepo of the core components that make this possible, including a codemirror client library, language server WebSocket server, and language server \"proxy.\"",
  "workspaces": [
    "./codemirror-ls",
    "./ls-ws-server",
    "./demo"
  ],
  "scripts": {
    "test": "vitest",
    "lint": "biome check",
    "format": "biome format --write .",
    "ci:tsc": "tsc -p ./ls-ws-server/tsconfig.json --noEmit && tsc -p ./codemirror-ls/tsconfig.json --noEmit"
  },
  "author": "Val Town",
  "license": "ISC",
  "devDependencies": {
    "@biomejs/biome": "2.1.4",
    "@changesets/cli": "^2.29.6",
    "happy-dom": "^18.0.1",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "vitest-websocket-mock": "^0.5.0"
  },
  "dependencies": {
    "@types/ws": "^8.18.1",
    "json-rpc-2.0": "^1.7.1",
    "msw": "^2.10.4",
    "pino": "^9.8.0"
  }
}
